# Copilot Instructions

## Project Guidelines
- The user is experiencing persistent client-side routing issues in their Next.js application. When clicking navigation buttons (Create Product, Edit Product, View Product), the page performs a full page refresh instead of client-side navigation. The replace_string_in_file tool is having issues. Need to use only final solution without intermediate edits.