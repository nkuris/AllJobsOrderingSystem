Client (Next.js)

Quick start:

- cd client-next
- npm install
- npm run dev

The frontend will start at http://localhost:3000. Configure the backend API URL via environment or by editing axios base URL in src/api.ts.
